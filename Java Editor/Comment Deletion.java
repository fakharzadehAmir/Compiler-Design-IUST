//import
public class Main {
    public static void main(String[] args) {
        // Single-line comment with an if structure
        // if (condition) {}

        // Single-line comment with a switch structure
        // switch(variable) {}

        // Single-line comment with a for loop structure
        // for (int i = 0; i < 10; i++) {}

        /* Multi-line comment with an if structure
           if (condition) {
               // some code
           }
        */

        /* Multi-line comment with a switch structure
           switch(variable) {
               case 1:
                   // some code
                   break;
               default:
                   // default code
           }
        */

        /* Multi-line comment with a for loop structure
           for (int i = 0; i < 10; i++) {
               // some repeated action
           }
        */
    }
}
